<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My To-Do List</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet"> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>What's your To-Do today?</h1>
    <div class="main">
    <form action = "/addtask" method = "POST" >
        <input id="abc" height="type="text" name="newtask" placeholder="Add a new task." />
        <button type="submit" class="btn btn-info">Add it!</button>

    </form>
    <form action = "/removetask" method = "POST" class="form1">
    <ul id="list">
    <% for(let i = 0; i < tasks.length; i++) { %>

        <li> <input type="checkbox" name="checkthat" value="<%= tasks[i] %>" /> <%= tasks[i] %> </li>
    <% } %>
      
    </ul>
    <button type="submit" class="btn btn-info">Remove tasks!</button>

    </form>
    <form action="/unremovetask" method="POST" class="form2">
        <div class="form2style">
        <h2> Completed Tasks: </h2>
        <ul id="list2">
            <% for(let i = 0; i < completed.length; i++) { %>
                <li>
                    <input type="checkbox" name="uncheck" value="<%= completed[i] %>" checked="true"/> <%= completed[i] %>
                </li>
            <% } %>
        </ul>
        <br>
        <button type="submit" class="btn btn-info">Unremove tasks!</button>
        </div>
        </div>
    </form>
</body>
</html>